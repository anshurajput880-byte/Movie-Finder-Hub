kind = "web"
previewPath = "/"
title = "CineMate — Movie Streaming"
version = "1.0.0"
id = "artifacts/cinemate"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20389

[services.development]
run = "pnpm --filter @workspace/cinemate run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cinemate", "run", "build" ]
publicDir = "artifacts/cinemate/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20389"
BASE_PATH = "/"
